import {Router} from 'express';
const router = Router();
import {householdData} from '../data/index.js';



router.route('/')
  .get(async (req, res) => {
})
 .post(async (req,res) => {

 })

export default router;